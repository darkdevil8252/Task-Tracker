# Task-Tracker
Task Tracker CLI is a minimalist, offline command-line tool for managing your to-do lists directly from the terminal. Built with Node.js and Commander.js, it uses the native fs module to persist tasks in a single JSON file without external servers or databases. It is installable globally via npm, providing a clean developer workflow. 
